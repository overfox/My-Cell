# Cloudflare Pages config for Helm.
# Static assets are served from the repo root; serverless code lives in
# functions/ (auto-detected as Pages Functions). No build step.
name = "helm"
pages_build_output_dir = "."
compatibility_date = "2024-09-01"
